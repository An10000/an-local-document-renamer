# an-local-document-renamer
Repository for a local document renamer. By default it reads arrival notice pdf and reads data to generate a new file name. Then it allows user to download the renamed file. 
